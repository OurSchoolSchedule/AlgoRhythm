# self-demo 시연 가이드

AlgoRhythm(**우리학교 시간표**)의 self-demo는 **개발용 로그인**부터 **관리자 대시보드**, **시간표 생성(데모 UI)**, **알림·보결 승인**, **교사 보결 요청·승인**, **투두·내역 등 추가 화면**까지 이어지는 핵심 사용자 흐름을 보여줍니다.

> **MVP 범위:** AI 채팅·자연어→LLM 파싱·실제 최적화 엔진은 **아직 연동되지 않았습니다.** 다만 해당 **화면·입력 UI는 구현**되어 있고 추후 연동 예정입니다. 시간표 생성 3단계의 로딩·대안 카드는 **프론트 목업**입니다.

## 시작 전 준비

- **시연 사이트**
  - 로컬: `cd frontend && npm run dev` → [http://localhost:5173](http://localhost:5173)
  - 배포(Vercel): [http://ourschoolschedule.vercel.app](http://ourschoolschedule.vercel.app)로 접속 (API는 `vercel.json`을 통해 [edu.rssolplan.com](https://edu.rssolplan.com)으로 프록시)
- **백엔드 API**: [https://edu.rssolplan.com](https://edu.rssolplan.com) (시드 DB·dev-token 사용 가능해야 함)
- **샘플 CSV**: [docs/schedule-teachers-sample.csv](docs/schedule-teachers-sample.csv)
- **자연어 제약 예시** (시간표 생성 2단계 입력란용 — **저장·파싱은 미연동**, 시연용 문구만):

  ```text
  김민지 선생님은 수요일 오전에 수업을 배정하지 않는다. 3학년 수업은 오전에 몰아서 배정한다.
  ```

### 데모 계정 (시드 DB 기준)

| 역할 | 이메일 |
|------|--------|
| 관리자 | `ownerA@example.com` |
| 교사1 | `staff1@example.com` |
| 교사2 | `staff2@example.com` |

> **참고**
>
> - 로그인은 MVP용 **dev-token**(이메일 → Access Token)입니다.
> - 실제 권한(승인 버튼 등)은 API의 `activeStore.position`(OWNER/STAFF) 기준입니다. 사이드바 하단 **관리자/교사** 토글은 **화면 미리보기용**이므로, 역할별 시연은 **계정을 바꿔 로그인**하는 것을 권장합니다.
> - 실제 학교·교사 개인정보가 담긴 CSV나 계정은 시연에 사용하지 마세요.

---

## 1. 로그인

![로그인 화면](docs/assets/1_login.png)

시연 사이트에 접속한 뒤 **AlgoRhythm** 개발용 로그인 화면에서 관리자 계정으로 들어갑니다.

1. 이메일 입력: `ownerA@example.com`
2. **로그인** 클릭
3. 헤더에 **우리학교 시간표**가 보이면 성공

로그인에 실패하면(`해당 이메일의 사용자를 찾을 수 없습니다`) 백엔드 기동·DB 시드(`backend/sql/createDB.sql`) 적용 여부를 확인하세요.

---

## 2. 관리자 홈 — 오늘 일정과 변동 브리핑

![관리자 홈](docs/assets/2_home.png)

로그인 직후 **홈** 화면에서 다음을 확인합니다.

- 상단 통계: **오늘 수업**, **이번 주 보결**, **등록 교사**
- 왼쪽 패널 **시간표** 탭: 요일·교시별 수업, 현재 교시 하이라이트
- **투두** 탭: 오늘 할 일(API 연동 시 체크 가능)
- 오른쪽 **실시간 변동 브리핑**: 보결·변경·완료 뱃지

이 단계에서는 **단톡·엑셀 없이** 변동을 한 화면에서 보는 UX가 핵심입니다.

---

## 3. 시간표 생성 — CSV 업로드와 제약 조건 (데모 UI)

![시간표 생성 1단계](docs/assets/3_schedule_step1.png)

홈의 **+ 시간표 생성하기** 또는 사이드바 **관리자 도구** → 시간표 생성으로 진입합니다.

### 3-1. 기본 설정

1. 학기명·기간 확인 (기본값 그대로 가능)
2. [docs/schedule-teachers-sample.csv](docs/schedule-teachers-sample.csv) 다운로드 후 **파일 선택**으로 업로드
3. **다음 →**

GitHub에서 이 문서를 볼 때는 CSV 파일 페이지에서 **Download raw file**로 저장하면 됩니다.

![시간표 생성 2단계](docs/assets/3_schedule_step2.png)

### 3-2. 제약 조건

1. 하드 제약 체크박스 2~3개 선택 (예: 1교시 수업 최소화, 점심시간 보장)
2. **교사 1일 최대 수업 교시** 슬라이더 조정
3. **자연어 제약 조건**에 위 예시 문장 붙여넣기 (UI 입력만, **LLM 파싱 미연동**)
4. **다음 →**

### 3-3. 생성 및 검토

![시간표 생성 3단계](docs/assets/3_schedule_step3.png)

1. 요약 항목 확인
2. **AI 시간표 생성 시작** 클릭 → 로딩(약 2초, **프론트 목업**)
3. 대안 A/B/C 중 **추천 대안(A)** 확인 후 **선택**
4. **확정 및 적용** (또는 관리자 도구로 이동)

아래가 보이면 이 구간 시연이 정상입니다.

![시간표 대안 결과](docs/assets/4_schedule_result.png)

- 3단계 스텝(기본 설정 → 제약 조건 → 생성 및 검토)
- 결과 카드: 대안 3종 + 점수(예: 94 / 88 / 82) — **실제 알고리즘 생성 아님**

---

## 4. 알림에서 보결·추가 근무 처리

![알림 패널](docs/assets/5_notifications.png)

헤더의 **알림** 아이콘을 눌러 오른쪽 패널을 엽니다.

1. **오늘** / **이번주** 그룹에서 보결·추가 근무 알림 확인
2. **승인** / **거절**(또는 교사용 **수락** / **거절**) 버튼이 있으면 **승인**으로 진행
3. 패널을 닫고 홈 **실시간 변동 브리핑**에 반영되는지 확인

알림이 비어 있으면 **5. 교사 보결 요청**을 먼저 진행한 뒤, 관리자 계정으로 다시 로그인해 이 단계를 시연하세요.

---

## 5. 교사1 보결 요청

![보결 요청](docs/assets/6_substitute_request.png)

1. 우측 상단 메뉴에서 **로그아웃**
2. `staff1@example.com`로 다시 로그인
3. 홈에서 **보결 요청하기** 클릭
4. **대상 수업** 선택 + **사유** 입력 후 제출

이번 주 등록된 수업이 없으면 `이번 주 등록된 수업이 없습니다`가 표시됩니다. 이 경우 DB에 해당 교사의 `work_shift` 시드가 있는지 확인하거나, 관리자 시연(3~4절)만으로 self-demo를 마무리해도 됩니다.

---

## 6. 교사2 보결 승인

![보결 승인](docs/assets/7_substitute_accept.png)

1. 우측 상단 메뉴에서 **로그아웃**
2. `staff2@example.com`로 다시 로그인
3. 알림에서 **수락** 클릭

---

## 7. 관리자 보결 최종 승인

![보결 최종 승인](docs/assets/8_substitute_finalapproval.png)

1. 우측 상단 메뉴에서 **로그아웃**
2. `ownerA@example.com`로 다시 로그인
3. 알림에서 **승인** 클릭

---

## 8. 추가 확인 — 투두·내역·기타 화면

보결 플로우(5~7절) 이후, 여유가 있으면 아래 화면을 짧게 둘러보시면 이외 기능을 확인하실 수 있습니다.

> **AI·LLM:** 백엔드·외부 API와 **연동하지 않았습니다.**  
> **UI:** 우하단 AI 채팅, 시간표 생성의 **자연어 제약 입력란**, **AI 시간표 생성** 버튼 등은 **화면만 구현**되어 있습니다. 버튼을 눌러도 실제 AI 응답·파싱·최적 생성은 동작하지 않거나 목업으로만 보입니다.

### 8-1. 투두 (API 연동)

| 위치 | 확인 항목 |
|------|-----------|
| **홈** → 왼쪽 패널 **투두** 탭 | 오늘 할 일 목록, 체크박스로 완료 토글 |
| **시간표** 메뉴 → **투두** 탭 | 같은 날짜 기준 할 일 (화면 레이아웃만 다름) |

- 항목이 없으면 `오늘 등록된 할 일이 없습니다` — DB·시드에 당일 todo가 있는지 확인
- 체크 시 API 반영되면 **실제 연동 구간**으로 시연 가능

![투두](docs/assets/9_todo.png)

### 8-2. 내역

사이드바 **내역**에서 학사 변동 이력을 조회합니다.

1. **내역** 메뉴 클릭
2. 상단 필터: **전체** / **보결** / **변경** / **생성** / **수정** / **교환** 등 선택
3. 날짜·유형 뱃지·처리 상태(완료/미처리) 확인

![내역](docs/assets/9_history.png)

### 8-3. 시간표·과목·수업 관리

| 메뉴 | 확인 항목 |
|------|-----------|
| **시간표** → **주간** | 요일×교시 그리드, 이번 주 수업 배치 |
| **과목·수업 관리** (관리자) | 학년 필터, 과목·담당 교사 표, CSV 업로드 UI |

![과목·수업 관리](docs/assets/9_management.png)

### 8-4. AI 관련 UI (연동 없음 · 화면만)

| UI | 위치 | 구현 현황 안내 |
|----|------|----------------|
| **AI 어시스턴트** | 화면 우하단 플로팅 버튼 | 채팅 창은 열리나 **외부 API 미연동** — 오류/안내 문구만 나올 수 있음 |
| **자연어 제약 조건** | 시간표 생성 2단계 | 입력·표시만 가능, **LLM 파싱·저장 없음** (3절 참고) |
| **AI 시간표 생성 시작** | 시간표 생성 3단계 | 로딩 후 대안 카드 = **프론트 목업** (실제 알고리즘 아님) |

![AI UI](docs/assets/9_ai_ui.png)

---

## 문제가 생겼을 때

| 증상 | 조치 |
|------|------|
| 로그인 실패 | 이메일 철자 확인 → DB 시드·`/api/auth/dev-token` 동작 확인 |
| 보결 요청 시 수업 없음 | `staff1@example.com` 주간 시프트 데이터 확인 또는 3~4절만 시연 |
| 알림·승인 버튼 없음 | 관리자 `ownerA@example.com`으로 로그인, 교사 요청 후 재로그인 |
| AI 채팅 오류 | **정상** — API 미연동. 8-4절 참고, 시연에서 생략 가능 |
| 투두가 비어 있음 | 당일 todo 시드·API 확인, 또는 8-1만 스킵 |

지원 범위를 벗어난 동작이면 MVP 미구현 영역일 수 있습니다. **샘플 CSV**, **시드 계정**으로 다시 시도해 주세요.

---

## 문의

이 가이드에 없는 내용이나 시연·기술 관련 문의는 아래로 연락해 주세요.

- **PM** [theancestor@ewhain.net](mailto:theancestor@ewhain.net)
- **FE** [zieyou52@ewha.ac.kr](mailto:zieyou52@ewha.ac.kr)
- **BE** [eceun@ewha.ac.kr](mailto:eceun@ewha.ac.kr)
